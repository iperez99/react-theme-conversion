import React, { Component } from 'react';

function Header(props) {
   return (
    <header id="header">
    <a className="logo" href="index.html">704 Yard Sale</a>
    </header>
   );
}

export default Header;